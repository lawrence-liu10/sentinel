# Pinned collections. Inventory is generated from Terraform (never edited).
collections:
  - name: community.docker
    version: ">=3.10.0,<5.0.0"
  - name: community.postgresql
    version: ">=3.4.0,<4.0.0"
  - name: amazon.aws
    version: ">=8.0.0,<10.0.0"
