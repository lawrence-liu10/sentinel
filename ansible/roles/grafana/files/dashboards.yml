# Dashboard provider — loads every JSON under the mounted dashboards dir.
apiVersion: 1

providers:
  - name: sentinel
    orgId: 1
    folder: Sentinel
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
