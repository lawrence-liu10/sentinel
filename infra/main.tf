# Main stack — composes no modules yet. Phase 0 keeps this a clean, empty
# skeleton so `terraform plan` reports 0 changes. The network / compute / iam /
# ssm modules under modules/ are wired in starting in Phase 1.
